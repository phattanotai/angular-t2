version: '3.4'
services:
  nginx-proxy:
    image: nginx-proxy
    build:
      context: ./
      dockerfile: ./Dockerfile
    restart: always
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - 80:80
